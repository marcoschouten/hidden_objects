\section{Conclusion}

We presented a scalable framework for extracting class-conditioned spatial priors from diffusion models, bypassing the limitations of sparse manual annotations and artifact-prone synthetic datasets. Our pipeline produces \textsc{HiddenObjects}, a dataset of 27M evaluated placements across 27k backgrounds that encodes human-aligned placement statistics. Distilling these priors into a lightweight model yields a massive speedup over the exhaustive search of the inpainting diffusion models. We outperform both dedicated placement models and zero-shot VLMs, and, notably, surpass sparse human annotations in downstream image editing quality. Our analysis further reveals that, while the extracted priors inherit certain photographic composition biases from their generative supervisors, they successfully enforce complex structural and scene constraints. We believe these explicit spatial priors offer a useful foundation for controllable image editing, data augmentation, and scene understanding.

% We introduced a scalable framework for extracting and explicitly modeling class-conditioned spatial priors from diffusion models, bypassing the constraints of sparse manual annotations and artifact-prone synthetic datasets. By coupling a diffusion-based inpainting pipeline with strict verifier supervision, we generated HiddenObjects—a high-fidelity dataset of 27 million evaluated object placements across 27,010 diverse backgrounds that uniquely encodes human-aligned placement statistics. Distilling this vast dataset into a lightweight object detector enabled fast, context-aware placement predictions, delivering a 686× speedup and 300× memory reduction for practical applications.
% Through extensive downstream evaluation, including plug-and-play object insertion and cross-dataset benchmarking, we demonstrated that our distilled priors substantially surpass existing placement models and generalist VLMs. Furthermore, our analysis of these priors reveals that while they naturally inherit certain photographic composition and perspective biases from their generative supervisors, they successfully learn to enforce complex structural and physical scene constraints. Ultimately, this work establishes a critical bridge between generative diffusion representations and explicit spatial reasoning, providing a powerful new foundation for scene understanding, controllable image editing, and vision-language grounding.